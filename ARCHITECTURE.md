# 项目架构文档

## 📋 概述

iFlow SDK Web Interface 是一个全栈 Web 应用，为 iFlow Python SDK 提供现代化的可视化界面。项目采用前后端分离架构，通过 WebSocket 实现实时双向通信。

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                         浏览器                                │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              React 前端应用                          │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐      │    │
│  │  │  UI 组件  │  │  Hooks   │  │  WebSocket   │      │    │
│  │  │          │  │          │  │   Client     │      │    │
│  │  └──────────┘  └──────────┘  └──────────────┘      │    │
│  └─────────────────────────────────────────────────────┘    │
└────────────────────────┬──────────────────────────────────┘
                         │ HTTP / WebSocket
                         │
┌────────────────────────▼──────────────────────────────────┐
│                    FastAPI 后端服务器                       │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────┐  │
│  │ REST API     │  │  WebSocket   │  │  Connection    │  │
│  │ Endpoints    │  │  Handler     │  │  Manager       │  │
│  └──────────────┘  └──────────────┘  └────────────────┘  │
│                                                             │
│  ┌───────────────────────────────────────────────────┐    │
│  │              iFlow SDK 集成层                      │    │
│  │  ┌──────────┐  ┌──────────┐  ┌───────────────┐   │    │
│  │  │  Client  │  │ Protocol │  │   Message     │   │    │
│  │  │  Wrapper │  │ Handler  │  │   Parser      │   │    │
│  │  └──────────┘  └──────────┘  └───────────────┘   │    │
│  └───────────────────────────────────────────────────┘    │
└────────────────────────┬──────────────────────────────────┘
                         │
                         │ Python API
                         │
┌────────────────────────▼──────────────────────────────────┐
│                      iFlow SDK                              │
│                                                             │
│  ┌──────────────────────────────────────────────────┐     │
│  │            Agent Communication Protocol           │     │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │     │
│  │  │ Message  │  │  Tool    │  │   Task       │   │     │
│  │  │ Stream   │  │  Calls   │  │   Planning   │   │     │
│  │  └──────────┘  └──────────┘  └──────────────┘   │     │
│  └──────────────────────────────────────────────────┘     │
└────────────────────────┬──────────────────────────────────┘
                         │
                         │ ACP Protocol
                         │
┌────────────────────────▼──────────────────────────────────┐
│                      iFlow CLI                              │
│                   (AI Agent 引擎)                           │
└─────────────────────────────────────────────────────────────┘
```

## 📁 目录结构详解

### 后端 (`/backend`)

```
backend/
├── __init__.py           # 包初始化文件
├── server.py             # FastAPI 主服务器
│   ├── FastAPI 应用配置
│   ├── CORS 中间件
│   ├── WebSocket 端点
│   ├── REST API 端点
│   ├── ConnectionManager 类
│   └── 消息处理函数
└── requirements.txt      # Python 依赖
```

#### 核心组件

**1. ConnectionManager**
- 管理活动的 WebSocket 连接
- 维护客户端会话状态
- 处理连接/断开事件

**2. WebSocket Handler**
- 接收客户端消息
- 路由到相应的处理函数
- 发送响应到客户端

**3. iFlow SDK Integration**
- 创建和配置 IFlowClient
- 处理流式消息
- 转换 SDK 消息到 WebSocket 格式

### 前端 (`/frontend`)

```
frontend/
├── public/              # 静态资源
│   └── vite.svg        # 图标
├── src/
│   ├── components/      # React 组件
│   │   ├── ChatInterface.tsx    # 聊天界面主组件
│   │   ├── MessageBubble.tsx    # 消息气泡组件
│   │   ├── Sidebar.tsx          # 侧边栏组件
│   │   └── ConfigPanel.tsx      # 配置面板组件
│   ├── hooks/          # 自定义 Hooks
│   │   └── useWebSocket.ts      # WebSocket 连接管理
│   ├── types.ts        # TypeScript 类型定义
│   ├── App.tsx         # 主应用组件
│   ├── main.tsx        # 应用入口
│   ├── index.css       # 全局样式
│   └── vite-env.d.ts   # Vite 类型定义
├── index.html          # HTML 模板
├── package.json        # 项目配置
├── tsconfig.json       # TypeScript 配置
├── vite.config.ts      # Vite 配置
├── tailwind.config.js  # Tailwind CSS 配置
└── postcss.config.js   # PostCSS 配置
```

#### 组件层次结构

```
App
├── Sidebar
│   ├── 连接状态指示器
│   ├── 工具调用列表
│   ├── 任务计划列表
│   └── 清空按钮
├── ChatInterface
│   ├── 消息列表
│   │   └── MessageBubble (多个)
│   └── 输入区域
└── ConfigPanel (条件渲染)
    ├── 权限模式选择
    ├── 进程管理选项
    └── 会话 ID 输入
```

## 🔄 数据流

### 1. 用户发送消息流程

```
用户输入
  ↓
ChatInterface 组件
  ↓
App.tsx (handleSendMessage)
  ↓
useWebSocket Hook
  ↓
WebSocket.send()
  ↓
后端 WebSocket 端点
  ↓
handle_chat_message()
  ↓
iFlow SDK Client
  ↓
iFlow CLI
```

### 2. AI 响应流程

```
iFlow CLI
  ↓
iFlow SDK (流式消息)
  ↓
后端消息处理
  ↓
WebSocket.send() (到客户端)
  ↓
useWebSocket Hook (onMessage)
  ↓
App.tsx (更新状态)
  ↓
React 重新渲染
  ↓
UI 更新 (流式显示)
```

## 📡 通信协议

### WebSocket 消息格式

#### 客户端 → 服务器

**聊天消息:**
```json
{
  "type": "chat",
  "message": "用户输入的文本",
  "config": {
    "permission_mode": "auto",
    "auto_start_process": true,
    "session_id": null
  }
}
```

**配置更新:**
```json
{
  "type": "config",
  "config": {
    "permission_mode": "confirm",
    "auto_start_process": true,
    "session_id": "session_123"
  }
}
```

#### 服务器 → 客户端

**连接确认:**
```json
{
  "type": "connection",
  "status": "connected",
  "client_id": "client_123",
  "iflow_available": true,
  "timestamp": "2025-12-30T10:00:00"
}
```

**AI 响应片段:**
```json
{
  "type": "assistant_chunk",
  "content": "响应文本片段",
  "timestamp": "2025-12-30T10:00:01"
}
```

**工具调用:**
```json
{
  "type": "tool_call",
  "tool_id": "tool_123",
  "label": "文件操作",
  "description": "创建文件 test.txt",
  "status": "running",
  "timestamp": "2025-12-30T10:00:02"
}
```

**任务计划:**
```json
{
  "type": "plan",
  "title": "任务标题",
  "tasks": [
    {"content": "步骤 1", "status": "completed"},
    {"content": "步骤 2", "status": "in_progress"}
  ],
  "timestamp": "2025-12-30T10:00:03"
}
```

**任务完成:**
```json
{
  "type": "task_finish",
  "timestamp": "2025-12-30T10:00:04"
}
```

## 🔐 安全考虑

### CORS 配置
- 开发环境: 允许所有来源 (*)
- 生产环境: 应限制为特定域名

### WebSocket 认证
- 当前: 基于客户端 ID
- 建议: 添加 JWT 或其他认证机制

### 工具调用权限
- 支持三种模式: auto, confirm, deny
- 用户可以控制工具执行权限

## 🎨 UI/UX 设计原则

### 响应式设计
- 移动优先
- 断点: sm (640px), md (768px), lg (1024px), xl (1280px)

### 颜色方案
- 主色: 蓝色 (primary)
- 成功: 绿色
- 警告: 黄色
- 错误: 红色
- 中性: 灰色系列

### 动画效果
- 消息渐入: fadeIn
- 加载状态: pulse
- 过渡: transition-colors

## 🚀 性能优化

### 前端优化
- React.memo 用于防止不必要的重渲染
- 虚拟滚动（如需要大量消息）
- 懒加载组件
- 代码分割

### 后端优化
- 异步处理所有 I/O 操作
- WebSocket 连接池管理
- 消息批处理（如适用）

### 网络优化
- WebSocket 保持连接
- 消息压缩（可选）
- 心跳检测

## 📊 可扩展性

### 水平扩展
- 使用 Redis 存储 WebSocket 连接状态
- 使用消息队列 (RabbitMQ, Redis Pub/Sub)
- 负载均衡器支持 WebSocket

### 垂直扩展
- 增加服务器资源
- 优化数据库查询
- 缓存常用数据

## 🧪 测试策略

### 单元测试
- 前端: Jest + React Testing Library
- 后端: pytest

### 集成测试
- API 端点测试
- WebSocket 连接测试
- iFlow SDK 集成测试

### E2E 测试
- Playwright 或 Cypress
- 用户流程测试

## 📚 技术栈版本

### 前端
- React: 18.3.1
- TypeScript: 5.6.3
- Vite: 6.0.1
- Tailwind CSS: 3.4.15
- Lucide React: 0.451.0

### 后端
- Python: 3.8+
- FastAPI: 0.115.0
- Uvicorn: 0.32.0
- WebSockets: 13.1
- iFlow SDK: 0.1.0+

## 🔮 未来改进

### 功能增强
- [ ] 用户认证和授权
- [ ] 会话历史持久化
- [ ] 多语言支持 (i18n)
- [ ] 主题切换 (深色模式)
- [ ] 语音输入/输出
- [ ] 文件上传支持
- [ ] 导出对话记录

### 技术改进
- [ ] 添加完整的测试套件
- [ ] 性能监控和日志
- [ ] Docker 容器化
- [ ] CI/CD 管道
- [ ] 数据库集成
- [ ] 缓存层

---

本文档将随项目发展持续更新。

